apiVersion: v1
kind: Service
metadata:
  name: recommendation-engine
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: recommendation-engine
---
apiVersion: v1
kind: Service
metadata:
  name: user-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: user-api
---
apiVersion: v1
kind: Service
metadata:
  name: trip-planner
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: trip-planner
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres
